import React from "react";
import { View, Text } from "react-native";

const TabBar = () => {
    return (
        <View>
            <Text>Custom TabBar</Text>
        </View>
    );
};

export default TabBar;
