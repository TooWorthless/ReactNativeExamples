import React from "react";
import { SafeAreaView, Text } from "react-native";

const RegisterScreen = () => {
    return (
        <SafeAreaView>
            <Text>Регистрация (заглушка)</Text>
        </SafeAreaView>
    );
};

export default RegisterScreen;
